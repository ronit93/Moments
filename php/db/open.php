<?php 
	/*
	// 1. Create a database connection
	error_reporting(0);
	$connection = mysql_connect("database_host","database_user","database_user_password");
	if (!$connection) {
		die("Database connected failed:" . mysql_error());
	}
	
	// 2. Select database to use
	$db_select = mysql_select_db("database_name", $connection);
	if (!$db_select) {
		die("Database selection failed:" . mysql_error());
	}
	*/
	
	/*
	
	//we are using a fake database for now, so put all variables here
	class FakeDatabase {
		
		public $username = "user212";
		public $password = "password212";
		
	}
	
	$db = new FakeDatabase();
	
	*/
	
	
?>